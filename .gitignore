# Local Claude Code settings - machine specific, not shared
.claude/settings.local.json

# OS clutter
Thumbs.db
ehthumbs.db
Desktop.ini
.DS_Store

# Editor
.vscode/
.idea/
*.swp

# Node, if a build step is ever added
node_modules/
dist/
build/

# ...but the single-file build of the service desk app is a deliverable:
# it is what you email to a colleague, and the repo is meant to need no
# build step. Rebuild it with `node service-desk/build.js` after changing
# anything under service-desk/assets/.
!service-desk/dist/
!service-desk/dist/*.html
npm-debug.log*
